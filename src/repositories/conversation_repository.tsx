// Import necessary libraries and components
import React from 'react';
import { Client, EmbeddingFunction } from 'chromadb';

// Define the component
const ConversationRepository: React.FC = () => {
  const [client, setCli